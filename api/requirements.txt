fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1

sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
pgvector==0.3.6

openai==1.57.0
tiktoken==0.8.0
pypdf==5.1.0
beautifulsoup4==4.12.3      # parser HTML pra módulo atas (comparador + corretor)
pydub==0.25.1               # split de áudio em chunks pro Whisper API (Fase 6 STT)
aiosmtplib==3.0.2           # SMTP async pra notificações do módulo atas (Fase 7)

# Connectors externos (Fase 6.2)
aioboto3==13.4.0           # AWS S3 (storage + connector)
azure-storage-blob==12.24.0  # Azure Blob (storage + connector)
openpyxl==3.1.5            # Excel/xlsx parser para connectors de planilha

# Bella Cobranças — Google Document AI (Fase 4)
google-cloud-documentai==2.32.0
google-cloud-storage==2.18.2
google-auth==2.36.0
pymupdf==1.24.14            # PyMuPDF/fitz — leitura/contagem de páginas de PDF

python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
email-validator==2.2.0
python-multipart==0.0.19

loguru==0.7.3
httpx==0.28.1

# Fila do worker de batch (scoring das capacidades de ML). Processo separado
# da API — ver worker/main.py. Usa o Redis que já estava no compose e ocioso.
rq==2.1.0
redis==5.2.1

# Testes
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
